import React from 'react'
import { createContext } from 'react';

// 1. Create-Context, every context is a provider, means it provide variable
const UserContext = createContext()  

export default UserContext;